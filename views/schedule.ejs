<ul>
    <% for (course of courses){%>
        <li> 
            <a href="/schedule/remove/<%= course._id %>">X</a>
            <%= course['Name'] %>
            <%= course.coursenum %>
            <%= course.name %>
            <%= course.term %>
            
        </li>
    <% } %>
</ul>

<!--
    <pre>
        <%= JSON.stringify(courses,null,5) %>
    </pre>
-->